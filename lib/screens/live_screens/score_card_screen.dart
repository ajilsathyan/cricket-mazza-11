import 'package:flutter/material.dart';

class ScoreCarScreen extends StatelessWidget {
  const ScoreCarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("ScoreCard"),),
    );
  }
}
