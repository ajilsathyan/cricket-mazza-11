import 'package:flutter/material.dart';

class BallsScreen extends StatelessWidget {
  const BallsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Balls Screen"),),
    );
  }
}
